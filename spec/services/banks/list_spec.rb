# frozen_string_literal: true

require 'spree_helpers/unit_spec_helper'

RSpec.describe Banks::List do
  describe 'Bank List Service' do
    # context 'When banks credentials are present' do
    #   before do
    #     allow(Rails.application.credentials).to receive(:banks)
    #   end

    #   it 'should list all banks' do
    #     bank_list_service = Banks::List.new
    #     response = bank_list_service.run

    #     expect(response).to be_an_instance_of(Hash)
    #   end
    # end
  end
end
